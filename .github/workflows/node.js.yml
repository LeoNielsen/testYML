name: My workflow

on: push

jobs:

  my-job:
    name: My job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: build
        uses: actions/setup-node@v3
        with:
         node-version: ${{ matrix.node-version }}
         cache: 'npm'
      - run: npm i
      - run: npm run build 
    
      - name: Deploy to luksuspanda.surge.sh
        uses: dswistowski/surge-sh-action@v1
        with:
          domain: 'luksuspanda.surge.sh'
          project: './dist'
          login: 'leo.nielsen21@gmail.com'
          token: '1cf59040d88689ec37effeabfb8ef411'
